---
// Mobile Bottom Navigation - Airbnb App Style
const currentPath = Astro.url.pathname;

// Helper function to check if current page is active
const isActive = (path: string) => {
  if (path === '/' && currentPath === '/') return true;
  if (path !== '/' && currentPath.startsWith(path)) return true;
  return false;
};
---

<!-- Mobile Bottom Navigation Bar -->
<div class="fixed bottom-0 left-0 right-0 z-50 bg-white border-t border-gray-200 md:hidden safe-area-bottom">
  <div class="grid grid-cols-5 h-16">

    <!-- Home -->
    <a
      href="/"
      class={`flex flex-col items-center justify-center space-y-1 transition-colors ${
        isActive('/') ? 'text-sba-navy' : 'text-gray-500'
      }`}
    >
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width={isActive('/') ? '2.5' : '2'}>
        <path stroke-linecap="round" stroke-linejoin="round" d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2V9z"/>
        <polyline points="9,22 9,12 15,12 15,22"/>
      </svg>
      <span class="text-xs font-medium">Home</span>
    </a>

    <!-- Properties -->
    <a
      href="/properties"
      class={`flex flex-col items-center justify-center space-y-1 transition-colors ${
        isActive('/properties') ? 'text-sba-navy' : 'text-gray-500'
      }`}
    >
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width={isActive('/properties') ? '2.5' : '2'}>
        <path stroke-linecap="round" stroke-linejoin="round" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/>
      </svg>
      <span class="text-xs font-medium">Properties</span>
    </a>

    <!-- Book Tour -->
    <a
      href="/book-consultation"
      class={`flex flex-col items-center justify-center space-y-1 transition-colors ${
        isActive('/book-consultation') ? 'text-sba-navy' : 'text-gray-500'
      }`}
    >
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width={isActive('/book-consultation') ? '2.5' : '2'}>
        <path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
      </svg>
      <span class="text-xs font-medium">Book</span>
    </a>

    <!-- Services -->
    <a
      href="/services"
      class={`flex flex-col items-center justify-center space-y-1 transition-colors ${
        isActive('/services') ? 'text-sba-navy' : 'text-gray-500'
      }`}
    >
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width={isActive('/services') ? '2.5' : '2'}>
        <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
      </svg>
      <span class="text-xs font-medium">Services</span>
    </a>

    <!-- About/Contact -->
    <a
      href="/about"
      class={`flex flex-col items-center justify-center space-y-1 transition-colors ${
        isActive('/contact') || isActive('/about') ? 'text-sba-navy' : 'text-gray-500'
      }`}
    >
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width={isActive('/contact') || isActive('/about') ? '2.5' : '2'}>
        <path stroke-linecap="round" stroke-linejoin="round" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
      </svg>
      <span class="text-xs font-medium">About</span>
    </a>

  </div>
</div>


<style>
  .safe-area-bottom {
    padding-bottom: env(safe-area-inset-bottom);
  }
</style>